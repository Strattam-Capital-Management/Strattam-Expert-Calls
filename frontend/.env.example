# Base URL of the Express/Node backend (no trailing slash).
NEXT_PUBLIC_API_BASE=http://localhost:4000

# Optional: only needed if you enable the lightweight Next.js page-gating cookie.
# Not required for the backend auth model itself (that uses the shared access
# code sent explicitly as X-Access-Code on every request).
ACCESS_CODE_SESSION_SECRET=change-me-in-production
